import { StudioHubShell } from "@/components/studio/StudioHubShell";

export default function ProjectHubLayout({ children }: { children: React.ReactNode }) {
  return <StudioHubShell>{children}</StudioHubShell>;
}