# Monorepo quick reference — copy env files per workspace:
#   cp backend/.env.example backend/.env
#   cp frontend/.env.example frontend/.env.local   # optional

# Backend (required): SQLite + partner keys + sync token — see backend/.env.example
# Frontend (optional): BACKEND_URL, PULSEFORGE_API_SECRET — see frontend/.env.example