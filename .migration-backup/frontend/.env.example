# Backend proxy target (default http://localhost:4000)
# BACKEND_URL=http://localhost:4000

# Optional — injected as X-PulseForge-Key on proxied /api/* (not /api/cloud/*)
# PULSEFORGE_API_SECRET=your_api_secret_here